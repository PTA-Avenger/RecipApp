<?xml version="1.0" encoding="UTF-8" ?>
<Application xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="RecipeApp.App">

    <Application.Resources>
        <ResourceDictionary>

            <!-- ✅ Theme-aware Brushes -->
            <SolidColorBrush x:Key="PrimaryBrush" Color="{AppThemeBinding Light=#6200EE, Dark=#BB86FC}" />
            <SolidColorBrush x:Key="BackgroundThemeBrush" Color="{AppThemeBinding Light=White, Dark=#1E1E1E}" />
            <SolidColorBrush x:Key="TextThemeBrush" Color="{AppThemeBinding Light=Black, Dark=White}" />

            <!-- ✅ Global Label style -->
            <Style TargetType="Label">
                <Setter Property="FontFamily" Value="Arial" />
                <Setter Property="FontSize" Value="16" />
                <Setter Property="TextColor" Value="{StaticResource TextThemeBrush}" />
            </Style>

            <!-- ✅ Global Button style -->
            <Style TargetType="Button">
                <Setter Property="BackgroundColor" Value="{StaticResource PrimaryBrush}" />
                <Setter Property="TextColor" Value="White" />
                <Setter Property="CornerRadius" Value="12" />
                <Setter Property="FontAttributes" Value="Bold" />
                <Setter Property="HeightRequest" Value="45" />
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>
